%h1 Stores

%table.table.table-striped
  %thead
    %tr
      %th Name
      %th Location
      %th
  %tbody
    -@all_stores.each do |store|
      %tr
        %td=store[:name]
        %td=store[:location]
        %td
          %a{ href: "/available?store_id=#{store[:store_id]}" } &#65284; View Stock
